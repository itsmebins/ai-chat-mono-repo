import { ChatContainer } from '@/components/Chat/ChatContainer';
import Layout from '@/layout';

const Home = () => {
  return (
    <Layout>
      <ChatContainer />
    </Layout>
  );
};

export default Home;
